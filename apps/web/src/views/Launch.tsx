import { Panel } from '../components/Panel.tsx';

export function LaunchView() {
  return (
    <Panel title="Launch">
      <p className="text-sm text-ink-3">Templates, the board and paused Meta campaigns arrive in phases 6 and 7. Every object will be created paused.</p>
    </Panel>
  );
}
